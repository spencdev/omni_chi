<?php

function omni_chi_preprocess_html(&$vars) {
	// To use the default omni_chi alpha layout in a subtheme,
	// add an 'omni-chi-alpha-default' class attribute to the
	// body tag in the subtheme's preprocess_html fx.
	// ex: $vars['classes_array'][] = 'omni-chi-alpha-default';
	// $vars['classes_array'][] = 'omni-psi-alpha-default';
}
