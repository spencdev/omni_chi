<?php

/**
 * Implements hook_process_page().
 */
function omni_chi_process_page(&$vars) {
  // You can use process hooks to modify the variables before they are passed to
  // the theme function or template file.
	//ddl($vars);
}
